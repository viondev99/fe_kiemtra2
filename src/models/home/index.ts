export interface ListColTableHome {
  id: string;
  name: string;
}

export interface DataTableHome {
  id: string;
  col1: string;
  col2: string;
  col3: string;
  col4?: string;
  col5?: string;
}
